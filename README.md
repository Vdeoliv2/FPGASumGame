# FPGASumGame
This system consists of a verilog program that creates a game on and FPGA board for the user to find a hexadecimal number to sum to a pseudo-random number trying to find a sum of 15. to obtain the hexadecimal number, the user. combines the switched on the board to find the binary equivalent to that number.
